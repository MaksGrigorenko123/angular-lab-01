import { Routes } from '@angular/router';
import { ItemsListComponent } from './features/items-list/items-list';

export const routes: Routes = [
  { path: '', component: ItemsListComponent }, // головна сторінка
];
 